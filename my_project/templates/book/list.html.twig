{% extends 'base.html.twig' %}

{% block title %}Список книг{% endblock %}

{% block body %}
    {% for book in books %}
        <div>
            <a href="{{ path('book_page', {'book': book.id}) }}">
                {{ book.title }}
            </a>
        </div>
    {% endfor %}
{% endblock %}
